1，使用缓存
2，测试前后端传递数据的方式,接收数据的方式。
3，使用mock模拟spring mvc 请求，进行单元测试。
4，使用文件存储，可以对数据进行存储，更新，修改。
5，spring mvc 过滤器，过滤不合理请求。

系统需求：
    涉及角色：admin--管理员,leader--教学领导,teacher---老师,student--学生
              course，location，score
    admin 可以给教师、学生设定界面权限。
        缓存不经常改变的信息
    教师登录，只能看到他教的课程，学生登录只能看到他选的课程。
    登录人员修改自己的个人信息、查看个人信息。
        上传图片
    教师排课没有时间、地点冲突。学生选课没有时间冲突。
    教师学期完成之后，给学生录入成绩，学生能查看自己的成绩。
        录入成绩导入
     自动统计教师年度、季度、月度工时、教学质量。
        定时任务，手工触发--消息

系统功能
   用户登录、菜单、按钮权限。
   数据库服务器，缓存服务器。
   webservice。
   缓存，ehcache,memcache,redis。
   数据库事物、定时任务、多线程。
   分布式服务例如：dubbo。
   消息队列。
   日志系统。